name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run NPM install
      run: npm install
    - name: Run NPM test
      run:  npm test
    - name: Run NPM build
      run: npm run build
